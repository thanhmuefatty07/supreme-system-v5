#!/usr/bin/env python3
"""
⚙️ Environment Configuration for Supreme System V5
i3-8th gen optimized with real API integration
"""

# === CRITICAL SYSTEM CONFIG ===
DEBUG = false
ENVIRONMENT = "production"

# === HARDWARE PROFILE ===
# Auto-detected: i3-8th gen + 4GB RAM
PROCESSOR_TYPE = "i3_8th_gen"
MEMORY_PROFILE = "4gb"
ENABLE_HARDWARE_OPTIMIZATION = true

# === API KEYS (REQUIRED) ===
# Replace with your real API keys for production data
ALPHA_VANTAGE_API_KEY = "your_alpha_vantage_key_here"
FINNHUB_API_KEY = "your_finnhub_key_here"
YAHOO_FINANCE_API_KEY = "optional_for_backup"

# === DEMO MODE ===
# Set to true if no real API keys available
USE_DEMO_MODE = true
DEMO_DATA_QUALITY = 0.8

# === TRADING CONFIGURATION ===
# i3-optimized settings (auto-adjusted by hardware profile)
MAX_TRADING_PAIRS = 5
UPDATE_FREQUENCY_MS = 2000
MAX_OPEN_POSITIONS = 3
MAX_POSITION_SIZE_USD = 500.0
MAX_DAILY_LOSS_USD = 50.0

# === AI COMPONENTS ===
# Automatically disabled on i3-4GB for memory conservation
USE_NEUROMORPHIC = true
USE_FOUNDATION_MODELS = false  # Disabled for i3-4GB
USE_MAMBA_SSM = false          # Disabled for i3-4GB
USE_ULTRA_LOW_LATENCY = true

# === SECURITY ===
JWT_SECRET_KEY = "your-super-secret-jwt-key-change-this-in-production-minimum-256-bits"
JWT_ALGORITHM = "HS256"
JWT_EXPIRATION_HOURS = 24

# === DATABASE ===
# SQLite for i3 (lightweight), PostgreSQL for production
DATABASE_URL = "sqlite:///./supreme_v5.db"
# For PostgreSQL: "postgresql://user:password@localhost/supreme_v5"

# === REDIS CACHE ===
# Lightweight config for i3-4GB
REDIS_URL = "redis://localhost:6379/0"
REDIS_MAX_MEMORY = "256mb"  # Conservative for i3

# === API SERVER ===
API_HOST = "0.0.0.0"
API_PORT = 8000
API_WORKERS = 1  # Single worker for i3
ENABLE_CORS = true

# === WEBSOCKET ===
WS_ENDPOINT = "/api/v1/stream"
WS_MAX_CONNECTIONS = 50  # Limited for i3
WS_HEARTBEAT_INTERVAL = 30  # Seconds

# === MONITORING ===
ENABLE_PROMETHEUS = true
PROMETHEUS_PORT = 9090
METRICS_RETENTION_HOURS = 12  # Reduced for i3

# === LOGGING ===
LOG_LEVEL = "INFO"
LOG_FORMAT = "structured"
LOG_FILE = "logs/supreme_v5.log"
MAX_LOG_SIZE_MB = 100
LOG_BACKUP_COUNT = 3

# === PERFORMANCE TARGETS ===
# Hardware-specific targets (auto-adjusted)
TARGET_API_LATENCY_MS = 100   # i3 target
TARGET_WS_LATENCY_MS = 2000   # i3 target
TARGET_MEMORY_USAGE_GB = 3.0  # i3 limit
TARGET_CPU_USAGE_PERCENT = 80 # i3 limit

# === EXCHANGE CONNECTIONS ===
# Demo mode settings
EXCHANGE_NAME = "binance_demo"
EXCHANGE_TESTNET = true
EXCHANGE_API_KEY = "demo_key"
EXCHANGE_API_SECRET = "demo_secret"

# === RISK MANAGEMENT ===
STOP_LOSS_PERCENT = 2.0
TAKE_PROFIT_PERCENT = 4.0
MAX_LEVERAGE = 1.0  # Conservative for i3
ENABLE_POSITION_SIZING = true

# === BACKTEST ===
BACKTEST_START_DATE = "2024-01-01"
BACKTEST_END_DATE = "2024-12-31"
BACKTEST_INITIAL_CAPITAL = 10000.0
BACKTEST_COMMISSION_RATE = 0.001  # 0.1%

# === DEVELOPMENT ===
ENABLE_RELOAD = false
ENABLE_PROFILING = false
ENABLE_DEBUG_ENDPOINTS = false

# === OPTIONAL INTEGRATIONS ===
# Uncomment when needed
# TELEGRAM_BOT_TOKEN = "your_telegram_bot_token"
# DISCORD_WEBHOOK_URL = "your_discord_webhook"
# SLACK_WEBHOOK_URL = "your_slack_webhook"

# === ADVANCED CONFIG ===
# Only modify if you know what you're doing
ASYNCIO_DEBUG = false
UVLOOP_ENABLED = true
GC_THRESHOLD = (700, 10, 10)  # Aggressive GC for i3