version: '3.8'

services:
  supreme-bot:
    build: .
    container_name: supreme_system_v5
    restart: unless-stopped
    volumes:
      - ./data:/app/data  # Persist logs and trade history
      - ./config:/app/config # Mount config for easy updates
    environment:
      - LOG_LEVEL=INFO
      - ENV=production
    # healthcheck: # Optional: Add healthcheck later
    #   test: ["CMD", "python", "scripts/healthcheck.py"]
    #   interval: 1m
    #   timeout: 10s
    #   retries: 3

  # Future: Add Prometheus/Grafana here